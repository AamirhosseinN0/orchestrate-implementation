# the register is working state for one run, not part of the skill
.claude/orchestration/
node_modules/
*.log
.DS_Store

# acceptance.sh reads a real recorded run from here. It is somebody
# else's project state — copy one in to run the harness, never commit it.
replay/
